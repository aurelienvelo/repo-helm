apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-exemple.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-exemple.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-exemple.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
